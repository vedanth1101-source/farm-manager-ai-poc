# ARTIFACT_STATUS

## Backend status
* Java 21 Spring Boot application fully tested.
* Endpoints `GET /api/questions` and `POST /api/query` are working.
* SQLite database populated with required tables.

## Frontend status
* React app compiles successfully with `npm run build`.
* Development server (`npm start`) serves page at `http://localhost:3000`.
* UI renders **Farm Manager AI**, input, Ask button, SQL and answer panels.
* Error panel displays backend failure messages.
*
## Build results
`npm run build` produced a production bundle in `frontend/build`
and compiled without errors.

## Completion percentage
- Frontend ready: 100 %
- Backend ready: 100 %
- Documentation: 100 % (README, DEMO_SCRIPT, ARTIFACT_STATUS)

### Remaining items
None. The demo is fully functional and all artifacts are committed.
